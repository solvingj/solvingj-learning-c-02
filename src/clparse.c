#include <clparse.h>

int parse_cli(const int argc, const char *argv[])
{
  for (i = 1; i < argc; i++)
  {
    printf("Something: %s", argv[i]);
  }

  for (i = 1; i < argc; i++)
  {
    printf("Something: %s", argv[i]);
  }
}

const char *argv[] 

int (const int argc, const char *argv[])
{
  int are_equal = strcmp("something i define", "the string passed in");
  int are_equal = strcmp("something i define", "the string passed in");

}


typedef struct option
{
  const char* id; //"help"
  const char* short_cut; // "H"
  int number_of_arguments; //0
}option;
